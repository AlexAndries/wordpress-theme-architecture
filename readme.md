# Wordpress Theme Architecture #

## Front End ##
Copy the files on `wp-content\themes\theme-name` and run in the same directory `compass watch --trace`

## Back End ## 
To generate the styles for login and admin, you can use `?compile=true` in url
